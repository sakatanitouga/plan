#ボールのx座標　ボールのy座標　紙コップx座標 紙コップy座標
position_history =[[0.0,0.0,0.0,0.0]]
def positon_manejar(master_duge,paper_cup,x,y):
{
    if master == True:{
     if len(position_history)>=0:{
         #ホンスワン
         if paper_cup==True:{
             #position_historyの末尾の紙コップのx座標y座標
         }else{
             #position_historyの末尾のボールのx座標y座標
         }
     }else{
        return position_history
     }
        
    }else{
        #熊谷さん
        #position_history配列の末尾にx,yを追加
        return position_history 
    }
}
aa =  positon_manejar(True,True,0,0)
printf(aa)
